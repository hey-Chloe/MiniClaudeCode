# Runtime dependencies. Keep compatible release ranges here; exact application
# environments can be frozen separately with `pip freeze` when deployed.
openai>=1.0,<3
python-dotenv>=1.0,<2
rich>=13.0,<15

# Test dependency retained for compatibility with the existing setup.
pytest>=8.0,<10

# tomllib backport for Python 3.10 (stdlib tomllib is available from 3.11).
tomli>=2.0; python_version < "3.11"
